export const dynamic = 'force-dynamic';

export default async function BlogPage() {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Blog - Static Test</h1>
      <p>If you can see this, the page renders. Sanity is not involved yet.</p>
    </div>
  );
}
